# CI/CD Pipeline для EDUPLATFORM

## 🚀 Автоматическое развертывание

При каждом пуше в `main` ветку автоматически:

1. **Сборка и тестирование**
   - Установка зависимостей Python
   - Запуск unit-тестов
   - Сборка Docker образов
   - Тестирование образов

2. **Деплой в Kubernetes**
   - Пуш образов в GitHub Container Registry
   - Обновление манифестов Kubernetes
   - Развертывание в кластер
   - Проверка статуса развертывания

## 🔧 Настройка секретов

В GitHub Repository → Settings → Secrets and variables → Actions:

1. **KUBECONFIG** - содержимое kubeconfig файла
2. **GITHUB_TOKEN** - автоматически создается GitHub

## 📊 Мониторинг

- GitHub Actions → для просмотра статуса сборки
- `kubectl get pods -n eduplatform` - для проверки подов
- `kubectl logs -f deployment/backend-api -n eduplatform` - логи бэкенда

## 🐛 Отладка

Если пайплайн падает:

1. Проверьте логи в GitHub Actions
2. Убедитесь что kubeconfig корректен
3. Проверьте что образы собираются без ошибок
4. Убедитесь что база данных запускается до бэкенда

## 🎯 Ручной запуск

```bash
# Запуск пайплайна вручную
git add .
git commit -m "feat: update application"
git push origin main
```

## 🔄 **Как запустить:**

```bash
# Создаем папку для тестов
mkdir -p backend/tests

# Добавляем все файлы
git add .
git commit -m "feat: add complete CI/CD pipeline with tests"
git push origin main
```